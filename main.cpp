#include <iostream>
using namespace std;

#include "CQueue.h"

int main()
{
    /* CQueue cq(3, 2);

    cq.in("hello");
    cq.in("world");
    cq.in("good");
    cq.in("morning");

    cq.outPrint();
    cq.outPrint();
    cq.outPrint();
    cq.outPrint();
    cq.outPrint(); */

    

    //(1)定义CGramCheck对象
    //(2)打开文件（若出错，则提示，并程序结束返回）
    //(3)读入文件内容，检查语法，并保存语法检查结果
    //(4)输出检查结果
    //(5)关闭文件
    return 0;
}
